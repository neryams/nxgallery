import { GalleryItemsDirective } from './gallery-items.directive';

describe('GalleryItemsDirective', () => {
  it('should create an instance', () => {
    // const directive = new GalleryItemsDirective();
    // expect(directive).toBeTruthy();
  });
});
